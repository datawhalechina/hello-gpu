# Hello GPU 主题主视觉

这些 WebP 来自用户确认的网页 GPT 效果图中的装饰画面，保留原始芯片与数据流的视觉形态；网页标题、导航、按钮、目录和教学图均使用真实 HTML/Vue/SVG 实现。

来源：`assets/ui-review/2026-09-12/`，原会话 https://chatgpt.com/c/6aa53c44-dba0-83ea-97bb-b7e9c0867822 。

- `gpu-hero-desktop.webp`：`01-home-dark.png` 的芯片画面。
- `gpu-hero-mobile.webp`：`07-mobile-home.png` 的芯片画面。
- `gpu-directory.webp`：`02-directory.png` 的抽象计算数据流。
- `gpu-chapter.webp`：`03-reading-dark.png` 的章节开篇装饰。

这些都是抽象计算结构示意，不是 RX 9070 XT 的实际芯片版图。图像由网页 GPT 生成，通过 Orca Computer Use 下载。独立媒体裁切和 WebP 压缩仅用于复用已确认画面，没有把网页截图作为页面实现。

## 2026-09-12 首页芯片更新

`hello-gpu-chip.png` 是同一网页 GPT 会话中新生成并下载的独立素材，已替代首页原先从设计图裁出的芯片。银色与石墨色分层芯片中央带有 `Hello-GPU` 字样，用于首页首屏和滚动叙事。

原始导出为 **1254 × 1254 RGBA PNG**，约 1.50 MiB。保留原始下载文件，没有放大插值。已检查 Alpha 范围为 0–255，53.28% 像素完全透明，并在白色和深色背景上检查边缘。构建生成的同名 WebP 为自动衍生文件；页面直接使用 PNG。

生成提示、原始副本、透明度检查记录和双背景预览位于 `assets/ui-review/2026-09-12/refinement/`。图中结构仍为装饰性计算示意，不对应特定硬件的实物结构。

# GPU Atlas 入口预览

`nvidia-rtx5090.webp` 原样取自用户提供的 `GPU-Atlas-source.zip` 内 `nvidia-atlas/src/assets/rtx5090-official-cutout.webp`，用于图谱入口的代表产品预览。原始来源和图片版权归属 NVIDIA；完整来源说明保存在 `apps/gpu-atlas/nvidia/src/assets/sources.json`。

AMD 卡片使用 CSS/SVG 绘制的抽象分层图形，表示筹备中的图谱内容，不是 AMD 实际芯片结构。

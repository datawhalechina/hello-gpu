# 第 2、3 章插画：wavefront 术语统一

核对日期：2026-09-10。图号对应用户目录中的 `1.png`–`12.png`，不是网页自动生成的图号。

当前状态：**6 张图片均已通过 Chrome 中的网页版 ChatGPT 修改、下载并逐图复核，已替换项目对应 PNG。** 用户原始 12 张图片保留，生成结果另存于用户图片目录的 `wavefront-edited/` 子目录；该目录中的 `generation-record.json` 记录了生成会话、原图与新图的 SHA-256 及项目目标路径。

本轮验证：`npm run docs:build` 通过，6 张 WebP 已重新生成；本地预览中第 2、3 章页面及 24 个 PNG / WebP 链接均返回 HTTP 200。项目中的 6 张 PNG 与下载结果哈希一致，用户原图哈希保持不变。

本次仅统一图中文字的术语。已修改 **03、05、07、08、10、12**，共 6 张、22 处中文旧译；01、02、04、06、09、11 无需因此重画。08 的箭头调整建议已由用户关闭，本轮保留现有箭头示意。生成结果均为 1672 × 941 像素；编号、数值、执行顺序和公式的复核结论见[插画审核记录](./illustration-review.md)。

| 图稿 | 主题 | 需替换处数 | 项目图片 |
| --- | --- | --- | --- |
| 03 | block、wave32 与 lane 编号 | 7 | `docs/part0-intro/chapter2/images/wave-lanes.png` |
| 05 | 软件分工与硬件执行 | 2 | `docs/part0-intro/chapter2/images/software-hardware.png` |
| 07 | 寄存器、LDS 与存储范围 | 6 | `docs/part0-intro/chapter3/images/storage-scopes.png` |
| 08 | 相邻与分散读取 | 1 | `docs/part0-intro/chapter3/images/coalesced-addresses.png` |
| 10 | 等待与延迟隐藏 | 5 | `docs/part0-intro/chapter3/images/latency-hiding.png` |
| 12 | WMMA 协作计算矩阵块 | 1 | `docs/part0-intro/chapter3/images/matrix-tile.png` |

以下每段指令可随对应原图单独发送到网页版 ChatGPT。使用用户最后确认的本地 PNG，避免以历史会话中的旧版本为底图。

## 03：block、wave32 与 lane 编号

```text
请编辑我上传的这张 GPU 教材插画，直接生成修改后的图片。保留清爽教材插画风格、原始横向比例和清晰度，只替换下列文字。全图中文术语“波前”统一为小写英文 wavefront，中英文之间留适当空格。英文较长时允许在原文字区域内合理换行或轻微缩小字体，不能挤压图形或裁掉内容。

1. 顶部副标题改为：在一个 block 中，线程既有“块内编号”，也有“wavefront 内编号”
2. 上方分组框右上角公式改为：256 ÷ 32 = 8 个 wavefront
3. 右上角说明框整段改为：一个 block 会被划分为多个 wavefront，每个 wavefront 包含 32 个线程。本例中 256 ÷ 32 = 8 个 wavefront。
4. 下方绿色总结框改为：块内编号：224–255；wavefront 内编号：0–31
5. 底部注释改为：本图只展示分组与编号，不表示这些 wavefront 同时执行。图形是逻辑分组示意，不是 GPU 物理版图。

保留所有图形、配色、连接线、wave 0–7 标签和 wave32 标注。放大区必须仍有完整的 32 个 lane：lane 0–31 依次对应线程 224–255；不得改动、漏掉或重复任何编号。保留 a[i]、b[i]、c[i]、加号、输入输出箭头，以及“i 是数组下标，不是 lane 编号”。修改后检查全图不再残留“波前”两个字。
```

## 05：软件分工与硬件执行

```text
请编辑我上传的这张 GPU 教材插画，直接生成修改后的图片。只做下面两处术语换字，保留原有清爽教材插画风格、横向比例、清晰度、配色、布局及全部图形：

1. 中间“调度执行”下方的说明改为：wavefront 的指令由硬件执行。
2. 左下方 thread / lane 区域标题改为：thread / lane：线程及其 wavefront 内位置

wavefront 必须全部小写，中英文留适当空格。英文较长时在原文字区域内合理换行或轻微缩小字体。其余文字、grid、block / workgroup、thread / lane、WGP、CU、SIMD、箭头、分组与包含关系全部保留。不要重新设计图。检查全图不再残留“波前”两个字。
```

## 07：寄存器、LDS 与存储范围

```text
请编辑我上传的这张 GPU 教材插画，直接生成修改后的图片。只统一术语文字，保留清爽教材插画风格、横向比例、清晰度、配色、布局和所有图形。全图中文术语“波前”统一为小写英文 wavefront；为避免替换后出现重复的括号释义，以下文字精确写为：

1. block / workgroup 标题下的副标题：由多个 wavefront 组成，共享同一块 LDS。
2. 左侧分组标题：wavefront A
3. 右侧分组标题：wavefront B
4. 左侧 SGPR 看板标题：SGPR：wavefront 共同使用的标量状态
5. 右侧 SGPR 看板标题：SGPR：wavefront 共同使用的标量状态
6. 底部右侧范围图例：看范围：各 lane / 整个 wavefront / 同一工作组

其余信息全部保留。尤其不得改动 VGPR 中的任何输入数字和四个输出结果 11、22、107、109，不得改变 lane 的独立数据、SGPR 的使用范围、LDS 的共享范围和全局内存的位置。英文较长时在原文字区域内换行或轻微缩小字体，不能覆盖图形。检查全图不再残留“波前”两个字。
```

## 08：相邻与分散读取

```text
请编辑我上传的这张 GPU 教材插画，直接生成修改后的图片。只修改底部“访存合并”的这一句文字，精确改为：

访存合并：把同一 wavefront 的地址请求放在一起看

wavefront 必须小写，中英文留适当空格。英文较长时允许在原文字区域内轻微缩小字号。

除此以外保持整张图原样：所有箭头的形状、方向、端点和位置保持原样，特别不要移动右侧 lane 0 或 lane 2 的箭头。保留相邻读取和分散读取中的所有 lane 标签、数组下标、0、17、34、51、颜色和格子。保留其他中文说明、原始横向比例、清晰度和教材插画风格。不要重新设计或修正其他区域。检查全图不再残留“波前”两个字。
```

## 10：等待与延迟隐藏

```text
请编辑我上传的这张 GPU 教材插画，直接生成修改后的图片。只将中文术语“波前”统一为小写英文 wavefront。保留原有清爽教材插画风格、横向比例、清晰度、灰色配色、表格布局和全部状态框。

1. 顶部标题下面的两行说明精确改为：当某个 wavefront 在等待数据时，如果有其他已驻留且就绪的 wavefront，执行资源会先去执行它；等到原来的 wavefront 数据到并重新就绪后，再选择它继续执行。
2. 表格左侧第一行标签改为：wavefront A
3. 表格左侧第二行标签改为：wavefront B

左侧标签如果一行过长，可在原标签框内分成两行，wavefront 在上、A 或 B 在下。其余文字和图形全部保留，尤其保留执行 A、等待数据、执行 B、数据就绪、之后继续执行 A 的先后关系，保留表示执行的粗边框和表示等待的斜线。保留下半部分资源占用的两组示意、资源限制说明，以及底部正确文字“这是概念示意”。不得重新绘制成固定硬件周期表。检查全图不再残留“波前”两个字。
```

## 12：WMMA 协作计算矩阵块

```text
请编辑我上传的这张 GPU 教材插画，直接生成修改后的图片。只修改中间公式框内 WMMA 后面的那一句文字，精确改为：

WMMA：wavefront 协作执行矩阵运算

wavefront 必须小写。英文较长时在原文字区域内合理缩小字体或换行，保持清晰可读。其他文字和图形全部保留：顶部“一个 wavefront 共同参与”、矩阵乘加公式 C ← A × B + C、左侧 A 和 B 及原来的 C 三个输入、右侧更新后的 C、所有箭头、蓝橙绿配色、人物、lane 标签、下方说明均保持原样。保留清爽教材插画风格、原始横向比例和清晰度。检查全图不再残留“波前”两个字。
```

## 生成后验收

- 保存生成结果到独立目录，先审核，再替换项目中的对应 PNG；保留用户原始图片。
- 核对 22 处术语替换和全图可读性，检查未出现错别字、重复编号、遗漏数字或裁切。
- 对照原图检查编号、算术、箭头、数据落点和存储范围；图 08 保留用户已认可的箭头。
- 替换通过审核的项目 PNG 后重新构建文档，生成对应 WebP，并确认第 2、3 章的图片链接可用。
